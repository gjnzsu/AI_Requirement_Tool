## 1. ai-rag-service Contract And Data Model

- [x] 1.1 Define request and response schemas for document upsert, semantic retrieval, document lookup, Jira-key context lookup, delete, and reindex operations.
- [x] 1.2 Define the supported business metadata fields for Jira issues and Confluence pages, including required fields by document type.
- [x] 1.3 Define technical metadata generated by `ai-rag-service`, including chunk IDs, chunk indexes, content hash, timestamps, embedding model, and schema version.
- [x] 1.4 Add validation for malformed metadata, unsupported filter operators, and missing required fields.

## 2. ai-rag-service Ingestion Lifecycle

- [x] 2.1 Implement idempotent document upsert using stable document IDs when supplied by the caller.
- [x] 2.2 Implement document ID generation for valid ingestion requests that omit a document ID.
- [x] 2.3 Ensure upsert refreshes active chunks for changed content without leaving duplicate active chunks.
- [x] 2.4 Preserve business metadata on all indexed chunks and add technical metadata during indexing.
- [x] 2.5 Add delete-by-document-ID support.
- [x] 2.6 Add reindex or refresh support for documents associated with a Jira key.

## 3. ai-rag-service Retrieval Lifecycle

- [x] 3.1 Implement semantic retrieval with optional metadata equality filters.
- [x] 3.2 Implement a small supported filter model, including equality and `in` semantics where the backing store supports them.
- [x] 3.3 Implement exact document lookup by document ID.
- [x] 3.4 Implement Jira-key contextual retrieval that returns the Jira issue and related Confluence pages.
- [x] 3.5 Return source-aware retrieval results containing content, rank or score, document ID, chunk ID, metadata, and source URL.
- [x] 3.6 Add clear empty-result behavior for missing Jira keys or filters with no matches.

## 4. ai-rag-service Tests

- [x] 4.1 Add contract tests for Jira issue ingestion and idempotent upsert.
- [x] 4.2 Add contract tests for Confluence page ingestion with `related_jira` metadata.
- [x] 4.3 Add tests for semantic retrieval with and without metadata filters.
- [x] 4.4 Add tests for Jira-key contextual retrieval across Jira and Confluence documents.
- [x] 4.5 Add tests for malformed filters and invalid metadata validation errors.
- [x] 4.6 Add tests for source-aware retrieval payload shape.
