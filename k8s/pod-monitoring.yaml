apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: ai-tool-metrics
spec:
  selector:
    matchLabels:
      app: ai-tool
  endpoints:
    - port: 5000
      path: /metrics
      interval: 30s
